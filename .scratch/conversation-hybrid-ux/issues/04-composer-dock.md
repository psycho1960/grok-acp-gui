# 04 — Composer 坞

**What to build:** The composer is one rounded dock: attach, a slash-command button (same menu as typing `/`), one model·reasoning control, and a single circular Send/Stop. While running the dock shows only Stop — never a second Send beside it. Model/reasoning drops its chevron when it cannot change.

**Blocked by:** 01 — 对话面挂上 Rose Pine Moon

**Status:** implemented

**Parent:** `docs/tasks/GAG-021-conversation-hybrid-ux.md`

- [x] Attach, `/ 指令`, `grok-x · 高`, and one circle live inside the dock
- [x] Slash button opens the same command list as typing `/`
- [x] Idle + content: circle sends; idle + empty: circle is inert
- [x] Running: circle is Stop only; no second send button in the dock
- [x] Model/reasoning is one control; locked state has no chevron
- [x] Shift+Enter still newlines; no header Stop duplicate
