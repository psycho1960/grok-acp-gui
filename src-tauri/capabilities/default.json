{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal Windows desktop capability for the Grok ACP GUI bootstrap shell",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "core:default",
    "dialog:default",
    "store:default"
  ]
}
